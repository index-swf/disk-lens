/// <reference types="vite/client" />

// Vite client 类型声明（标准脚手架文件）。
// 提供 `import.meta.env`（含 DEV / PROD 标志）等前端环境变量的类型。
// 缺它时 `import.meta.env` 会被 TS 报 "Property 'env' does not exist on type 'ImportMeta'"。
