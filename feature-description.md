# 项目名称：USN 极速磁盘扫描器 (USN Scanner)

## 1. 项目概述
开发一款类似于 TreeSize Free 的 Windows 桌面工具。核心功能是对选定的磁盘驱动器（如 C:\）进行空间占用分析。
**核心亮点**：无需管理员权限（UAC），利用 NTFS 的 USN（更新序列号）日记进行极速扫描，实现 10 秒内完成数百 GB 数据的统计与可视化。

## 2. 技术架构
- **开发语言**：前端（Web 技术），后端（Rust）。
- **开发框架**：**Tauri**（用于将 Web 页面打包成桌面应用，调用系统自带的 WebView2，保证安装包体小于 10MB）。
- **前端框架**：React 或 Vue 3 + Vite。
- **前端图表库**：ECharts（基于其强大的 Treemap 矩形树图组件，用于展示空间占用）。
- **后端底层依赖**：`windows-rs`（微软官方 Rust 绑定库，用于调用 Windows 底层 API）。

## 3. 核心功能需求

### 3.1 基础扫描能力
- 用户可选择任意 NTFS 格式的磁盘盘符（例如 `"C:\\"`）。
- 后台采用 **USN 日记** 方案进行扫描。

### 3.2 数据聚合逻辑（重点）
- 扫描完成后，不显示每个单独的文件，**而是按照文件夹进行合并**。
- 例如：一个文件夹下有 100 个文件，总计占用 1GB，结果展示为该文件夹下有一个虚拟的 `[Files]` 条目，大小为 1GB。
- 需要计算准确的**文件夹占用大小**（包含子文件夹的大小）、**分配大小**（Allocated Size，即实际占据磁盘簇的大小）、以及**文件夹内的文件数量和文件夹数量**。

### 3.3 数据可视化展示
- 默认展示顶层的**矩形树图（Treemap）**，矩形面积对应文件大小。
- 支持**下钻**操作：点击树图中的某个文件夹，进入其子目录并重新渲染树图。
- 支持**回溯**操作：能方便地回到上级目录。
- 展示清晰的数据表格：名称、大小（Size）、实际占用（Allocated Size）、文件数、文件夹数、上级占比（% of Parent）。

### 3.4 状态反馈
- 扫描过程中，前端界面应显示“正在扫描...”的加载状态。
- 扫描结束后，实时更新数据和图表。

## 4. 后端 Rust 核心算法详细说明（Agent 需重点关注）

### 4.1 调用 Windows API
使用 `windows-rs` 库引入以下 Win32 API：
1. `CreateFileW`：以 `GENERIC_READ` 权限打开盘符（如 `\\.\C:`），获取句柄 `hVolume`。**特别注意：这是读取 USN 日志，不需要管理员权限。**
2. `DeviceIoControl`：配合控制码 `FSCTL_QUERY_USN_JOURNAL` 来获取当前磁盘的 USN 记录基本信息（如起始记录号 `StartUsn`）。
3. `DeviceIoControl`：配合控制码 `FSCTL_ENUM_USN_DATA` 来枚举 USN 记录数据。该 API 会返回一个二进制的 `USN_RECORD_V2` 或 `USN_RECORD_V3` 数据流。

### 4.2 数据结构定义（Rust Struct）
- 扫描结果的数据结构必须对应 ECharts Treemap 的格式：
```json
{
  "name": "C:\\",
  "size": 268435456000, // 总占用字节数
  "allocated_size": 280000000000, // 分配大小
  "file_count": 1000,
  "folder_count": 100,
  "children": [
    {
      "name": "Users",
      "size": 150000000000,
      "children": [
        { "name": "[Files]", "size": 5000000000, "file_count": 500 },
        { "name": "Default", "size": 145000000000, "children": [] }
      ]
    }
  ]
}

```

### 4.3 USN 数据的解析与树形组装逻辑（重点难点）
由于 USN 数据提供的是**扁平化**的记录列表，包含`FRN`（文件引用号，即文件的唯一 ID）和 `ParentFRN`（父文件夹的唯一 ID），后端必须用 Rust 实现以下算法：
1. **遍历记录并建立映射表**：建立一个 `HashMap<FRN, FileNode>`，包含记录中的文件名、大小、父级 FRN 等信息。
2. **构建层级树**：遍历上述 HashMap，将子节点挂载到父节点对应的 `children` 数组中。
3. **聚合与折叠**：遍历构建好的树，对于每个文件夹，做以下操作：
   - 累加所有直接子文件和子文件夹的大小，更新该文件夹的 `size`（大小）。
   - 创建虚拟的 `[Files]` 节点，将当前文件夹下**所有直接文件**移入这个 `[Files]` 节点中。
4. **按大小排序**：最终返回给前端前，对树中每个节点的 `children` 列表，按 `size` 从大到小进行降序排列，确保最大的文件和文件夹排在最前。

## 5. 前端页面设计说明
- 布局：左侧为树形列表表格，右侧/下方为矩形树图区域。
- 点击树形表中的行时，应触发渲染对应文件夹的树图。
- 提供百分比进度条或加载动画（扫描未结束时禁用操作）。

## 6. 前端与后端交互（Tauri Command）
定义 Tauri 的 `invoke` 接口（Rust 导出给前端）：
- **接口名**：`scan_drive`
- **入参**：`{ drive_path: String }`
- **出参**：返回符合 ECharts 格式的 `NodeData` 结构体，序列化为 JSON。
- **调用方式**：前端使用 `invoke('scan_drive', { drive_path: 'C:\\' }).then(...)` 接收数据。

## 7. 开发进度阶段性拆解（Agent 分步生成）
- **第一步（1天）**：搭建 Tauri + Vue/React 脚手架，验证前后端 IPC 通信（返回 Hello World 字符串）。
- **第二步（2-3天）**：在 Rust 中实现 `windows-rs` 调用 `FSCTL_ENUM_USN_DATA` API，打印出原始 USN 记录数据到控制台，确认能成功读取卷的元数据。
- **第三步（2-3天）**：Rust 实现 USN 数据到树形结构（HashMap -> Tree）的转换算法，并加入 `[Files]` 合并逻辑，输出一份 JSON。
- **第四步（1天）**：前端引入 ECharts，将 JSON 注入 Treemap，实现基本显示。
- **第五步（2天）**：实现点击下钻/返回功能，优化界面 UI 和 CSS，添加加载动画。
- **第六步（1天）**：打包 Tauri 应用，测试 Windows x64 编译版本并交付。

## 8. 注意事项与边界条件（Agent 需写进代码防御逻辑）
- **USN 日记未开启**：如果因为某些杀毒软件或系统设置导致 USN 日记被清空或关闭，API 可能返回空数据。第一版如果遇到这种情况，可以报错提示用户“当前磁盘未开启 USN 日记，请提权使用 MFT 扫描（第二版功能）”，或者干脆退化为传统的 `read_dir` 普通扫描（虽然慢，但不至于让应用崩溃）。
- **文件路径深度**：Rust 中处理 Windows 路径时注意长路径问题（`\\?\` 前缀），避免解析时出现路径截断。
- **性能**：由于 USN 返回的二进制流可能很大，批量处理时，Rust 代码务必使用迭代器（Iterator）流式解析，不要在内存中一次性存储所有字节数据，以防内存溢出。


### 💡 给你的额外小建议
> **“请务必在 Rust 代码中使用 `windows-rs` 的 `DeviceIoControl` 配合 `FSCTL_ENUM_USN_DATA`，并注意需要将 USN 记录中的 `FileName` 解析为 UTF-16 字符串。遇到数据时，不要一次性将整个 USN 流读进内存，请使用循环分批读取。”**
